[package]
name = "blitztest"
version = "0.1.0"
edition = "2024"
# Test consumer / internal harness, not a product.
publish = false

[[bin]]
name = "blitztest"
path = "src/main.rs"

[dependencies]
regex = "1"
